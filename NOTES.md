# Текущее состояние

- Включен RU/EN переключатель языка, переводы лежат в `src/i18n/strings.ts`, провайдер в `src/i18n/LanguageProvider.tsx`.
- Блог работает на одной странице: мини-карточки переключают большой пост без перехода на отдельный URL.
- Добавлены изображения постов в `public/blog/`, подключены в `src/content/posts.ts`.
- Блок Docs переработан: левое меню с поиском, секции, карточки, метрики-заглушки, новые стили.
- В Docs hero стоит фон `public/docs-hero.jpg` (из `references/Лого /пум .jpg`) + затемняющий градиент.
- Для docs-hero усилены читабельность текста и кнопки.

# Где смотреть

- Docs страница: `src/app/docs/page.tsx`, стили `src/app/globals.css`.
- Переводы: `src/i18n/strings.ts`.
- Посты/картинки: `src/content/posts.ts`, `public/blog/`.

# Что делать дальше

- Визуальный аудит всех секций (hero, capabilities, process, team, blog, docs) и правка контраста/иерархии.
- Полировка текста (орфография/формулировки) по всем страницам.
- Наполнить Docs реальными метриками и кейсами (когда будут цифры/гайды).
- При необходимости оптимизировать изображения (webp/размеры).
